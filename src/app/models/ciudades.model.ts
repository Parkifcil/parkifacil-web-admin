export interface CiudadReador {
    id: number;
    strCiudad: string;
}

export interface CiudadResponseReador {
    responseCode: number;
    responseText: string;
    data: CiudadReador[];
}

export class CiudadesModel {
 // clase anterior remplazar por los modelos de arriba
 // una vez esten completos  
}